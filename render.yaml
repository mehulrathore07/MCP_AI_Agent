services:
  - type: web
    name: hr-leave-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agent_server:app --host 0.0.0.0 --port 8000
    plan: free
    autoDeploy: true
